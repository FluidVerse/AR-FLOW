namespace InteractionMenus.MenuElements {
    /// <summary>
    /// Number of digits to be displayed in the label next to the slider.
    /// </summary>
    public enum LabelDigits {
        Two,
        Three,
        Four,
        Five,
        Six
    }
}