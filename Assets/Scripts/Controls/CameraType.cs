namespace Controls {
    /// <summary>
    /// Defines the different camera modes in the application, which can be switched between using the
    /// <see cref="CameraManager"/>.
    /// </summary>
    public enum CameraType {
        Fixed,
        SplinePath,
        DetailView,
        AerialView
    }
}