namespace Shaders {
    /// <summary>
    /// Transparency modes used for controlling the transparency of an object using the
    /// <see cref="TransparencyController"/> script.
    /// </summary>
    public enum TransparencyMode {
        Opaque,
        Transparent
    }
}